<UserControl x:Class="FitFileOverlay.Controls.LoadTemplateDialogContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FitFileOverlay.Controls"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:LoadTemplateDialogContent, IsDesignTimeCreatable=False}"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Width="400" Height="200">
        <Border BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                Background="{DynamicResource CardBackground}">
            <ui:ListView ItemsSource="{Binding Templates}"
                         SelectedValue="{Binding SelectedValue}"
                         SelectedIndex="{Binding SelectedIndex}"
                         SelectionMode="Single">
                <ui:ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Key}" 
                                   Margin="5,2,5,2"/>
                    </DataTemplate>
                </ui:ListView.ItemTemplate>
            </ui:ListView>
        </Border>
    </Grid>
</UserControl>
